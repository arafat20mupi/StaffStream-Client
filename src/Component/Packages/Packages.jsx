import PaymentSection from "./PaymentSection";

const Packages = () => {
    return (
        <section className="container mx-auto my-32 lg:px-0 px-5">
            <PaymentSection/>
        </section>
    );
};

export default Packages;